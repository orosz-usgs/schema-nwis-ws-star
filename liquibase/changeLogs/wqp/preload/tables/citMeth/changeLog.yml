databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql
    -runningAs:
        username: ${NWIS_WS_STAR_OWNER}

  - changeSet:
      author: drsteini
      id: "create.table.cit_meth"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: cit_meth
      changes:
        - sqlFile:
            path: citMeth.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists cit_meth cascade;
