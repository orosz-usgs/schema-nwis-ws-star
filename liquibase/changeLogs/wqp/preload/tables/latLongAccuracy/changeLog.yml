databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql
    -runningAs:
        username: ${NWIS_WS_STAR_OWNER}

  - changeSet:
      author: drsteini
      id: "create.table.lat_long_accuracy"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: lat_long_accuracy
      changes:
        - sqlFile:
            path: latLongAccuracy.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists lat_long_accuracy cascade;
