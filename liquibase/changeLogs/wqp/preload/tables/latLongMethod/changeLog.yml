databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql
    -runningAs:
        username: ${NWIS_WS_STAR_OWNER}

  - changeSet:
      author: drsteini
      id: "create.table.lat_long_method"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: lat_long_method
      changes:
        - sqlFile:
            path: latLongMethod.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists lat_long_method cascade;
