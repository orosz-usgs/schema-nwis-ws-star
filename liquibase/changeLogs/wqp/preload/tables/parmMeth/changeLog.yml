databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql
    -runningAs:
        username: ${NWIS_WS_STAR_OWNER}

  - changeSet:
      author: drsteini
      id: "create.table.parm_meth"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: parm_meth
      changes:
        - sqlFile:
            path: parmMeth.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists parm_meth cascade;
