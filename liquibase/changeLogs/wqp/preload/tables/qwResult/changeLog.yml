databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql
    -runningAs:
        username: ${NWIS_WS_STAR_OWNER}

  - changeSet:
      author: drsteini
      id: "create.table.qw_result"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: qw_result
      changes:
        - sqlFile:
            path: qwResult.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists qw_result cascade;
