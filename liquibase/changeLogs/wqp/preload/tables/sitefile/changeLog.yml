databaseChangeLog:
  - preConditions:
    - dbms:
        type: postgresql
    -runningAs:
        username: ${NWIS_WS_STAR_OWNER}

  - changeSet:
      author: drsteini
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: sitefile
      id: "create.table.sitefile"
      changes:
        - sqlFile:
            path: sitefile.sql
            relativeToChangelogFile: true
        - rollback: drop table if exists sitefile cascade;
