databaseChangeLog:
  - preConditions:
    - dbms:
        type: oracle

  - changeSet:
      author: drsteini
      id: "create.table.lat_long_method"
      preConditions:
        - onFail: MARK_RAN
        - onError: HALT
        - not:
          - tableExists:
              tableName: lat_long_method
      changes:
        - sqlFile:
            path: latLongMethod.sql
            relativeToChangelogFile: true
        - rollback: drop table lat_long_method cascade constraints purge;
